<header class="page-heading prediction-heading">
    <div><span class="eyebrow">Facts first. Estimates second.</span><h1>AI notes you can question.</h1><p>Current market facts stay separate from uncertain estimates, with signal, probability, confidence, risk, timestamp, and reasoning in plain language.</p></div>
    <div class="prediction-disclaimer"><strong>Educational analysis only</strong><span><?= e(config('app.disclaimer')) ?></span></div>
</header>

<?php if (!empty($generationSummary['generated'])): ?><div class="notice success" role="status"><?= (int) $generationSummary['generated'] ?> missing estimate<?= (int) $generationSummary['generated'] === 1 ? '' : 's' ?> generated automatically.</div><?php endif; ?>
<?php if (!empty($generationSummary['failed'])): ?><div class="notice warning" role="status">Some estimates could not be refreshed because a market or AI provider was temporarily unavailable. Existing saved estimates remain visible and each card can be retried.</div><?php endif; ?>

<nav class="segmented-tabs" role="tablist" aria-label="Prediction sections">
    <button role="tab" data-prediction-tab="owned" aria-selected="true">My stocks <span><?= count($ownedPredictions) ?></span></button>
    <button role="tab" data-prediction-tab="watchlist" aria-selected="false">Watchlist <span><?= count($watchlistPredictions) ?></span></button>
    <button role="tab" data-prediction-tab="opportunities" aria-selected="false">Opportunities <span><?= count($opportunities) ?></span></button>
    <button role="tab" data-prediction-tab="history" aria-selected="false">History <span><?= count($history) ?></span></button>
</nav>

<?php
$renderPrediction = static function(array $prediction, string $context): void { ?>
    <article class="prediction-card">
        <div class="fact-estimate-legend"><span class="fact-label">Real market facts</span><span class="estimate-label">Uncertain AI estimate</span></div>
        <div class="prediction-card-header"><div><span class="ticker-tile"><?= e(substr($prediction['symbol'],0,2)) ?></span><div><strong><?= e($prediction['symbol']) ?></strong><span><?= e($prediction['company_name']) ?></span><small><?= e(ucfirst($context)) ?> · generated <?= e(date('d M H:i',strtotime($prediction['generated_at']??'now'))) ?></small></div></div><span class="signal <?= e($prediction['signal']) ?>"><?= e(strtoupper($prediction['signal'])) ?></span></div>
        <div class="probability-grid"><div class="probability-main"><span>Estimated chance of increasing</span><strong><?= e(number_format((float)$prediction['estimated_probability_up'],1)) ?>%</strong><small>over <?= e($prediction['horizon']) ?> · uncertain estimate</small></div><div><span>Estimated down</span><strong><?= e(number_format((float)$prediction['estimated_probability_down'],1)) ?>%</strong></div><div><span>Confidence</span><strong><?= e(number_format((float)$prediction['confidence_score'],1)) ?>%</strong></div><div><span>Risk</span><strong><?= e(ucfirst($prediction['risk_level'])) ?></strong></div></div>
        <div class="analyst-note"><span class="eyebrow">Concise analyst note</span><p class="prediction-summary"><?= e($prediction['summary']) ?></p></div>
        <div class="score-bars"><label>Technical <span><?= e(number_format((float)$prediction['technical_score'],1)) ?>/100</span><progress max="100" value="<?= e($prediction['technical_score']) ?>"></progress></label><label>News <span><?= $prediction['news_score']!==null?e(number_format((float)$prediction['news_score'],1)).'/100':'Unavailable' ?></span><progress max="100" value="<?= e($prediction['news_score']??0) ?>"></progress></label><label>Market <span><?= $prediction['market_score']!==null?e(number_format((float)$prediction['market_score'],1)).'/100':'Unavailable' ?></span><progress max="100" value="<?= e($prediction['market_score']??0) ?>"></progress></label></div>
        <details><summary>Factors and invalidation conditions</summary><div class="factor-columns"><div><strong>Positive factors</strong><ul><?php foreach($prediction['positive_factors'] as $factor):?><li><?= e($factor) ?></li><?php endforeach;?></ul></div><div><strong>Negative factors</strong><ul><?php foreach($prediction['negative_factors'] as $factor):?><li><?= e($factor) ?></li><?php endforeach;?></ul></div><div><strong>Invalidated if</strong><ul><?php foreach($prediction['invalidation_conditions'] as $factor):?><li><?= e($factor) ?></li><?php endforeach;?></ul></div></div></details>
        <div class="prediction-meta"><span>Data: <?= e($prediction['source_data_timestamp']??'Unknown') ?></span><span>Model: <?= e($prediction['model_name']) ?></span><span class="freshness <?= e($prediction['status']) ?>"><?= e(ucfirst($prediction['status'])) ?></span></div>
        <div class="stock-actions"><button class="button primary" data-prediction-action="buy" data-symbol="<?= e($prediction['symbol']) ?>">Simulated buy</button><button class="button secondary" data-prediction-action="watch" data-stock-id="<?= (int)$prediction['stock_id'] ?>">Add to watchlist</button><button class="button ghost" data-prediction-action="refresh" data-stock-id="<?= (int)$prediction['stock_id'] ?>">Refresh estimate</button></div>
        <p class="card-disclaimer"><?= e(config('app.disclaimer')) ?></p>
    </article>
<?php };
?>

<section class="prediction-panel" data-prediction-panel="owned">
    <div class="section-heading"><div><h2>Owned-stock analysis</h2><p>The page automatically creates a technical first estimate from real market history. Refresh a card to add current AI/news context.</p></div></div>
    <?php if($ownedPredictions):?><div class="prediction-list"><?php foreach($ownedPredictions as $prediction)$renderPrediction($prediction,'owned');?></div><?php else:?><div class="empty-state"><strong>No owned-stock predictions yet.</strong><p>Open Stocks, choose an owned position, and generate an estimate.</p><a class="button primary" href="<?= e(url('/stocks')) ?>">Open Stocks</a></div><?php endif;?>
</section>

<section class="prediction-panel" data-prediction-panel="watchlist" hidden>
    <div class="section-heading"><div><h2>Watchlist analysis</h2><p>Every watchlisted idea receives a fast technical first estimate; Refresh estimate adds OpenRouter/news context.</p></div></div>
    <?php if($watchlistPredictions):?><div class="prediction-list"><?php foreach($watchlistPredictions as $prediction)$renderPrediction($prediction,'watchlist');?></div><?php else:?><div class="empty-state"><strong>No watchlist predictions yet.</strong><p>Generate one from a watchlist card in Stocks.</p><a class="button primary" href="<?= e(url('/stocks?tab=watchlist')) ?>">Open watchlist</a></div><?php endif;?>
</section>

<section class="prediction-panel" data-prediction-panel="opportunities" hidden>
    <div class="section-heading"><div><h2>Limited-universe opportunities</h2><p>Up to five non-owned, non-watchlisted stocks from a fixed liquid demo universe. This is not a whole-market scan.</p></div><button class="button primary" id="discover-predictions">Refresh opportunity estimates</button></div>
    <?php if($opportunities):?><div class="opportunity-grid"><?php foreach($opportunities as $opportunity):$stock=$opportunity['stock'];$prediction=$opportunity['prediction'];?><article class="opportunity-card"><div class="opportunity-number"><?= e(str_pad((string)($loopIndex??1),2,'0',STR_PAD_LEFT)) ?></div><div><strong><?= e($stock['symbol']) ?></strong><span><?= e($stock['company_name']) ?></span></div><p><?= e($opportunity['selection_reason']) ?></p><?php if($prediction):?><div class="opportunity-signal"><span class="signal <?= e($prediction['signal']) ?>"><?= e(strtoupper($prediction['signal'])) ?></span><strong>Estimated <?= e(number_format((float)$prediction['estimated_probability_up'],1)) ?>% up over <?= e($prediction['horizon']) ?></strong><small><?= e(number_format((float)$prediction['confidence_score'],1)) ?>% confidence · <?= e(ucfirst($prediction['risk_level'])) ?> risk</small></div><?php else:?><div class="prediction-unavailable">Estimate not generated yet.</div><?php endif;?><div class="stock-actions"><button class="button secondary" data-prediction-action="watch" data-stock-id="<?= (int)$stock['id'] ?>">Add to watchlist</button><button class="button primary" data-prediction-action="buy" data-symbol="<?= e($stock['symbol']) ?>">Simulated buy</button><button class="button ghost" data-prediction-action="refresh" data-stock-id="<?= (int)$stock['id'] ?>">Detailed estimate</button></div></article><?php $loopIndex=($loopIndex??1)+1;endforeach;?></div><?php else:?><div class="empty-state"><p>No eligible discovery candidates remain in the configured universe.</p></div><?php endif;?>
</section>

<section class="prediction-panel" data-prediction-panel="history" hidden>
    <div class="section-heading"><div><h2>Prediction history & outcomes</h2><p>Every generated estimate is stored. Outcomes remain pending until the stated horizon ends and a valid end price is available.</p></div></div>
    <?php if($history):?><div class="table-scroll"><table><thead><tr><th>Generated</th><th>Stock</th><th>Horizon</th><th>Signal</th><th>Estimated up</th><th>Confidence</th><th>Start</th><th>End</th><th>Actual</th><th>Outcome</th></tr></thead><tbody><?php foreach($history as $item):?><tr><td><?= e(date('d M Y H:i',strtotime($item['generated_at']))) ?></td><td><strong><?= e($item['symbol']) ?></strong></td><td><?= e($item['horizon']) ?></td><td><span class="signal <?= e($item['signal']) ?>"><?= e(strtoupper($item['signal'])) ?></span></td><td><?= e(number_format((float)$item['estimated_probability_up'],1)) ?>% / <?= e($item['horizon']) ?></td><td><?= e(number_format((float)$item['confidence_score'],1)) ?>%</td><td><?= $item['start_price']!==null?e(number_format((float)$item['start_price'],2)):'—' ?></td><td><?= $item['end_price']!==null?e(number_format((float)$item['end_price'],2)):'—' ?></td><td><?= $item['actual_change_percent']!==null?e(percent((float)$item['actual_change_percent'])):'—' ?></td><td><?= e(ucfirst((string)($item['outcome']??'pending'))) ?></td></tr><?php endforeach;?></tbody></table></div><?php else:?><div class="empty-state"><p>No prediction history yet.</p></div><?php endif;?>
</section>

<dialog id="prediction-buy-dialog" class="app-dialog"><form method="dialog" class="dialog-close-form"><button aria-label="Close">×</button></form><div><span class="eyebrow">Paper trade</span><h2>Simulated buy</h2><p id="prediction-buy-symbol"></p></div><form id="prediction-buy-form" class="stack-form"><input type="hidden" name="symbol"><label>Quantity<input type="number" name="quantity" min="0.00000001" step="0.00000001" required></label><button class="button primary full" type="submit">Confirm simulated buy</button><small>The server obtains and validates the current quote before recording a paper transaction.</small></form></dialog>
